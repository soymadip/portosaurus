---
title: "Installation"
description: "How to install the project."
icon: "🛠️"
slug: "/install"
---

This file is named `1 - installation.mdx`, so it will automatically appear first inside the Getting Started category!

_(Notice the `slug: "/install"` property in the frontmatter above? It cleanly overrides the default URL routing so this page is accessible at exactly `/docs/install`!)_

## Prerequisites

- Node.js
- A terminal

## Steps

1. Clone the repository
2. Run `npm install`
3. Profit!
